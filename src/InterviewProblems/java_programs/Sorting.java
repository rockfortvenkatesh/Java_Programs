import java.util.Comparator;
import java.util.List;

public class Sorting {

    public static void main(String[] args) {
       // List<String> li = List.of("Chennai","Bengaloru","pune");

       // li.stream().sorted(Comparator.naturalOrder()).forEach(System.out::println);
 //  List<Integer> num = List.of(1,1,2);

  // num.stream().distinct().forEach(System.out::println);

       // emplyee a, b

        //employee.stream().collect(GroupingBy::a).forEach(System.out::println);

    }
}
